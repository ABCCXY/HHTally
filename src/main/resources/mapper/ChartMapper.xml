<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ylc.hhtally.mapper.ChartMapper">
    <select id="getMonthSum" resultType="Double">
        select money from hhtally.bill where month(time)=#{month} and userId=#{userId}
    </select>

    <select id="getDaySum" resultType="Double">
        select money from hhtally.bill
        where year(time)=#{year} and month(time)=#{month} and day(time)=#{day} and userId=#{userId}
    </select>

    <select id="getMonthSumIncome" resultType="java.lang.Double">
        select money from hhtally.bill where month(time)=#{month} and userId=#{userId} and money <![CDATA[<]]> 0
    </select>
</mapper>